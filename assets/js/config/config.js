define(function (require) {

    'use strict';

    var angular = require('angular'),
            folder = 'config/',
            config = angular.module('app.config', []);

    config
            //routes
//        .config('routes', require('config/routesConfig'))
            ;

    return config;
});