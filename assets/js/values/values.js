define(function (require) {

    'use strict';

    var angular = require('angular'),
            values = angular.module('app.values', []);

    values
            //routes
//        .value('value', require('values/value'))
            ;

    return values;
});