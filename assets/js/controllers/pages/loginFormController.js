define(function () {

    'use strict';

    function ctrl(
            $scope,
            $http
            ) {


        $scope.autenticar = function (frm) {
            
            
            
        };

    }

    ctrl.$inject = [
        '$scope',
        '$http'
    ];

    return ctrl;

});